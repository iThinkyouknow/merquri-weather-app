export const localStorageSavedSearchHistoryKey = 'savedSearchHistory';
export const queryKey = 'q';

export const statusCodes = {
  ok: 200
};

export const unitType = navigator.language === 'en-US'
  ? 'imperial'
  : 'metric';